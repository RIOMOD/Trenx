<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?android:attr/windowBackground"
    android:padding="20dp"
    android:fitsSystemWindows="true">

    <TextView
        android:id="@+id/tvDetailName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/exercise_name_placeholder"
        android:textColor="?android:attr/textColorPrimary"
        android:textSize="28sp"
        android:textStyle="bold"
        android:layout_marginTop="20dp"
        tools:text="TÊN BÀI TẬP"/>

    <TextView
        android:id="@+id/tvDetailReps"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/exercise_reps_placeholder"
        android:textColor="@color/accent_color"
        android:textSize="18sp"
        android:layout_marginTop="8dp"
        tools:text="3 Sets x 15 Reps"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/technique_guide"
        android:textColor="?android:attr/textColorSecondary"
        android:textStyle="bold"
        android:layout_marginTop="32dp"/>

    <TextView
        android:id="@+id/tvDetailDesc"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="@string/exercise_desc_placeholder"
        android:textColor="?android:attr/textColorPrimary"
        android:textSize="16sp"
        android:lineSpacingExtra="5dp"
        android:layout_marginTop="12dp"
        tools:text="Mô tả chi tiết cách thực hiện bài tập sẽ hiển thị ở đây..."/>

    <Button
        android:id="@+id/btnStartWorkout"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="@string/start_workout"
        android:backgroundTint="@color/accent_color"
        android:textColor="#000000"
        android:textStyle="bold"
        android:textSize="18sp"/>
</LinearLayout>